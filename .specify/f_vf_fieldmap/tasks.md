# Tasks: f_vf_fieldmap

**Spec**: `.specify/f_vf_fieldmap/spec.md`
**Plan**: `.specify/f_vf_fieldmap/plan.md`
**Build order**: Sequential. Complete each phase before next.
**Branching**: All work on main. Commit after each phase checkpoint passes.

---

## Expected Source Layout

```
~/Vsynth/patterns/                    (not in repo — scratch only)
  fieldmap-scratch.maxpat             — Phase 1 codebox verification patch

/Users/matt/Github/f_/
  .specify/f_vf_fieldmap/
    spec.md                           — done
    plan.md                           — done
    tasks.md                          — this file
    definition.py                     — written end of Phase 2

  patchers/
    f_vf_fieldmap.maxpat              — generated by build_patcher.py (Phase 2)

  docs/
    f_vf_fieldmap.md                  — as-built reference (Phase 4)
```

---

## Phase 1: Codebox — scratch patch verification

**Purpose**: Verify the gradient math compiles and produces correct output before writing any patcher JSON. Gate on `vec()` syntax.

⚠️ BLOCKING: Do not proceed to Phase 2 until all Phase 1 checkpoints pass.

- [x] T001 Create `fieldmap-scratch.maxpat` in `~/Vsynth/patterns/` with vs_render, jit.gl.pix @type float32, jit.pwindow, and a solid-color jit.gl.texture source wired to pix in0
- [x] T002 Write codebox v1 in scratch pix — luminance composite, central difference using `vec(norm.x + scale, norm.y)` for UV offsets, `strength` and `scale` Params, f_vecfield encoding (RG = grad*0.5+0.5, B=0.5, A=1.0), bypass constant 0.5 output
- [x] T003 Verify `vec(norm.x + scale, norm.y)` syntax compiles and produces correct UV offset — confirmed working, no fallback needed
- [x] T004 Test flat input (solid color source) → confirmed output is exactly 0.5/0.5 in RG channels
- [x] T005 Swap source to jit.gl.bfg (Perlin noise, medium frequency) → confirmed visible RG gradient field in jit.pwindow
- [x] T006 Patch pix outlet into vs_displacement — deferred to Phase 3 integration
- [x] T007 Test `scale` param low vs high — confirmed low produces fine-structure gradient, high produces coarser/smoother gradient
- [x] T008 Test `strength = 0` → confirmed neutral field output; `strength` high → clamp confirmed clean
- [x] T009 Calibrate `strength` default: 4.0 confirmed usable with BFGener8r Perlin at defaults
- [x] T010 Test bypass: confirmed constant 0.5/0.5/0.5/1.0 output when bypass = 1

**Checkpoint**: Flat input → exactly 0.5/0.5. Perlin input → visible gradient. vs_displacement warp follows noise contours. scale and strength params produce expected effects. bypass outputs neutral field. `vec()` syntax confirmed or fallback documented.

---

## Phase 2: definition.py + patcher build

**Purpose**: Write the definition file from the confirmed codebox and generate the production patcher.

- [x] T011 Read `tools/spec.md` to confirm definition.py schema; read `f_vf_vortex/definition.py` as format reference for a 2-param f_vf_ producer
- [x] T012 Write `.specify/f_vf_fieldmap/definition.py` — name `f_vf_fieldmap`, prefix `fieldmap`, object_name `fieldmap_pix`, type `float32`, processor archetype (single texture inlet), confirmed codebox content, params: `strength` (0.0–10.0, default 4.0) and `scale` (0.001–0.05, default 0.004), bypass, presentation layout
- [x] T013 Run `python3 tools/build_patcher.py .specify/f_vf_fieldmap/definition.py` — no errors, `patchers/f_vf_fieldmap.maxpat` written
- [x] T014 Validate generated JSON: `python3 -c "import json; json.load(open('patchers/f_vf_fieldmap.maxpat'))"` — parses clean
- [x] T015 Open `f_vf_fieldmap.maxpat` in Max within a Vsynth session — loads without errors, presentation panel visible, params addressable
- [x] T016 Verify unconnected inlet: neutral field output confirmed, no crash

**Checkpoint**: Patcher loads in Vsynth. Params addressable. Unconnected inlet produces neutral field. JSON validates.

---

## Phase 3: Integration with f_caustic

**Purpose**: Verify the three-patch signal chain produces compelling output with varied inputs.

- [x] T017 Patch `jit.gl.bfg (simplex) → f_vf_fieldmap → f_caustic` — confirmed caustic bright bands accumulate along noise ridge lines; lens-like convergence zones with tight bright bands visible; `strength` and `scale` performable
- [x] T018 Swap jit.gl.bfg to Voronoi smooth mode → confirmed distinct caustic character; sharp radial fan structures from directional Voronoi cell gradients; valid f_vecfield output
- [x] T019 Swap jit.gl.bfg to fractal.fbm mode → confirmed valid output; distinct fine-grained scattered caustic character; fbm gradient higher contrast than simplex — needs lower strength (0.08 vs 4.0) and adjusted scale (0.02)

**Checkpoint**: Three-patch signal chain works with at least Perlin and Voronoi inputs. Caustic structure visible and responsive to `strength`/`scale` params.

---

## Phase 4: Docs + README

**Purpose**: Promote f_vf_fieldmap from planned to working in all project docs.

- [x] T020 Write `docs/f-reference/f_vf_fieldmap.md` — param table, signal chain, calibration table by input type, notes
- [x] T021 Update `docs/f-reference/f_vecfield_type.md` — f_vf_fieldmap row changed from Planned to Complete
- [x] T022 Update `README.md` — f_vf_fieldmap added to f_vf_ family table
- [ ] T023 Commit: `feat: f_vf_fieldmap — scalar texture to vecfield via central difference gradient`

**Checkpoint**: Docs written. README updated. Git clean. Commit done.

---

## Dependencies & Execution Order

### Phase dependencies
- Phase 1 → Phase 2 → Phase 3 → Phase 4 (strictly sequential)
- Phase 1 is the only real unknown — once codebox confirmed, remaining phases are mechanical

### Within Phase 1
- T001–T002 must complete before any verification tasks
- T003 (`vec()` syntax) must resolve before T004–T010 (all depend on working sample offsets)
- T009 (calibrate strength default) may update the default in spec and plan — do this before T012

### Within Phase 2
- T011 (read reference) → T012 (write definition.py) → T013 (build) → T014 (validate JSON) → T015–T016 (verify in Max)

### Parallel opportunities
- None — patch is simple enough that sequential is cleaner than parallel

---

## Notes

- Scratch patch lives in `~/Vsynth/patterns/` — not committed. Keep it for calibration reference.
- If `vec()` fails (T003): update plan ADR 5 with the fallback form used, update codebox in T002 record, continue. No other tasks affected.
- If `strength` default needs adjustment after T009: update `.specify/f_vf_fieldmap/spec.md` clarifications and plan ADR 3 before writing definition.py in T012.
- `scale` lower bound 0.001 approaches sub-pixel; GPU interpolation handles it gracefully but gradient becomes noisy. Note in docs if observed during T007.
- Total tasks: 23
