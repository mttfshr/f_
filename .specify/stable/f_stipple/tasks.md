# Tasks: f_stipple

**Spec**: `.specify/f_stipple/spec.md`
**Plan**: `.specify/f_stipple/plan.md`
**Build order**: Sequential. Complete each phase before next. Codebox must be confirmed working before build script.

---

## Expected Source Layout

```
~/Vsynth/patterns/              (not in repo — scratch and build artifacts)
  stipple-scratch.maxpat        — scratch patch for codebox iteration
  build_f_stipple.py            — build script (written in Phase 3)

/Users/matt/Github/f_/
  patchers/
    f_stipple.maxpat            — final output, generated by build script
  .specify/f_stipple/
    spec.md                     — done
    plan.md                     — done
    tasks.md                    — this file
  ideas/
    f_stipple.md                — graduated reference spec
  docs/
    f_stipple.md                — written in Phase 4 (post-build)
```

---

## Phase 0: Scratch Patch Setup

**Purpose**: Working jit.gl.pix in Vsynth context, ready for codebox iteration.

- [x] T001 Create `stipple-scratch.maxpat` in `~/Vsynth/patterns/` — minimal jit.gl.pix wired to Vsynth context with render trigger texture on inlet 0
- [x] T002 Confirm patch renders output in Vsynth (black is fine — just confirm the GL context fires)

**Checkpoint**: Can paste codebox content and see output update in real time in Vsynth.

---

## Phase 1: Core Hash Field

**Purpose**: Coordinate rotation, two hash functions, anisotropy blend, threshold/softness. No drift, no processor mode yet.

- [x] T003 Write codebox — coordinate rotation: `along`/`across` from `norm` rotated by `angle` (theta = angle * PI/180)
- [x] T004 Write codebox — sin-based hash on `along` axis: `h_parallel = sin(along * freq * 43758.5453); h_parallel = h_parallel - floor(h_parallel)`
- [x] T005 Write codebox — arithmetic hash combining both axes: `h_iso = sin((along * 127.1 + across * 311.7) * freq); h_iso = h_iso - floor(h_iso)`
- [x] T006 Write codebox — anisotropy blend: `hash_field = mix(h_iso, h_parallel, anisotropy)`
- [x] T007 Write codebox — threshold/softness: `stipple = smoothstep(threshold - softness * 0.5, threshold + softness * 0.5, hash_field)`
- [x] T008 Write codebox — source mode output: `out1 = vec(stipple, stipple, stipple, 1.0)` with bypass (`mix(out, vec(0,0,0,1), bypass)`)
- [x] T009 Paste codebox into scratch patch, add Param declarations for `freq`, `angle`, `anisotropy`, `threshold`, `softness`, `bypass`
- [x] T010 Verify Block 1: visible stipple at defaults; freq sweeps grain size; angle rotates orientation; anisotropy blends lines↔grain; threshold/softness control boundary

**Calibration (record findings for build script):**
- [x] T011 Determine `freq` default and useful range — 0.0–20.0, default 5.0. Linear mapping sufficient.
- [x] T012 Confirm 2D hash in `h_iso` breaks the diagonal seam — confirmed clean at tested angles.

**Checkpoint**: Block 1 verification passes. Freq range and seam behavior documented.

---

## Phase 2: Drift + Processor Modes

**Purpose**: Add time-based drift, source/processor auto-detect, proc_mode (density/displacement), correct bypass per mode.

- [x] T013 Write codebox — phase offset: add `Param along_phase(0.0)` and `Param across_phase(0.0)`; evaluate hash at `(along + along_phase, across + across_phase)`. No internal time — drive externally same pattern as f_masonry `phase`.
- [x] T014 Verify drift: nonzero `along_phase` scrolls field; confirmed with phasor~ → snapshot~ driving the param
- [x] T015 Write codebox — processor/density mode: `input_luma = sample(in1, norm).r`; `local_thresh = mix(threshold - input_luma * 0.5, threshold, step(0.5, proc_mode))`
- [x] T016 Write codebox — processor/displacement mode: `warp = input_luma * threshold`; evaluate hash at warped coords
- [x] T017 Write codebox — proc_mode branch via `step(0.5, proc_mode)` and `mix()`
- [x] T018 Write codebox — src_mode branch: source bypass = black, processor bypass = pass input through
- [x] T019 Add `Param proc_mode(0.0)` and `Param src_mode(0.0)` to codebox Param declarations
- [x] T020 Paste updated codebox into scratch patch; verify Block 2

**Calibration (record findings for build script):**
- [x] T021 Phase scale: externally driven, no internal multiplier needed
- [x] T022 Processor/density compositing: multiply stipple against input color (`src_col * stipple`) — confirmed working

**Checkpoint**: Block 2 verification passes. Speed scale and compositing method decided and locked in codebox.

---

## Phase 3: Build Script + Patcher

**Purpose**: Generate `f_stipple.maxpat` via Python build script using confirmed codebox as source of truth.

- [x] T023 Write `build_f_stipple.py` — superseded by general `tools/build_patcher.py`
- [x] T024 Add `jit.gl.pix vsynth @name stipple_pix` with gen subpatcher and confirmed codebox
- [x] T025 Add `vs_inState` wiring for dual-mode archetype
- [x] T026 Add `route freq angle anisotropy threshold softness zoom along_phase across_phase colorize proc_mode` to build script
- [x] T027 Add `live.dial` objects for all float params with correct ranges
- [x] T028 Add `live.numbox` for `proc_mode`
- [x] T029 Add `bypass_toggle.js` jsui — NOTE: param_connect omitted; causes Max crash on load when set in generated JSON. Bypass works via prepend wire only. State save via autopattr.
- [x] T030 Add `autopattr @varname stipple_autopattr`
- [x] T031 Add `moduleSize.js` chain
- [x] T032 Add `parameters` block — bypass jsui excluded (no param_connect)
- [x] T033 Run build script; load `f_stipple.maxpat` in Max — loads cleanly
- [ ] T034 Verify autopattr saves/restores state across close/reopen

**Checkpoint**: Block 3 verification passes. Patch loads cleanly in Vsynth, all params addressable by name message.

---

## Phase 4: Integration + Docs

**Purpose**: Validate in real signal chains; write docs; update project state.

- [ ] T035 Integration test: f_stipple source mode → f_droste (confirm texture passes through cleanly)
- [ ] T036 Integration test: f_stipple processor/density with incoming WFG texture (confirm halftone effect visible)
- [ ] T037 Integration test: f_stipple processor/displacement with incoming WFG texture (confirm warp effect visible)
- [ ] T038 Check frame rate with f_stipple in chain — acceptable performance?
- [ ] T039 Write `docs/f_stipple.md` — as-built reference: params table, signal chain, usage notes, known issues
- [ ] T040 Update `README.md` — add f_stipple to bpatcher status table (Working), update build queue
- [ ] T041 Update `ideas/f_stipple.md` — mark as built, link to docs
- [ ] T042 Commit: `git add -A && git commit -m "add f_stipple — 2D hash field stipple source/processor"`

**Checkpoint**: Block 4 verification passes. README updated. Docs written. Committed.

---

## Dependencies & Execution Order

**Phase dependencies (strict):**
- Phase 0 → Phase 1 → Phase 2 → Phase 3 → Phase 4
- No phase can begin until the previous checkpoint passes

**Parallel opportunities:**
- T003–T008 (codebox sections) can be written together as a single codebox text before pasting — they are one artifact
- T023–T032 (build script sections) can be written as one Python script before running — they are one artifact
- T035–T038 (integration tests) can be run in any order

**Known decision gates (must resolve before proceeding):**
- T011 (freq range) must be resolved before T027 (live.dial ranges in build script)
- T021–T022 (speed scale, compositing) must be resolved before T027–T028

---

## Notes

- Codebox content from Phase 1/2 is the source of truth for Phase 3 — do not rewrite in the build script, copy it verbatim
- Max does not pick up external file edits while patch is open — close without saving, then reopen to load build script output
- If build script produces a patch that crashes Max on load, read the saved .maxpat JSON before editing — object IDs shift on every save
- `proc_mode` auto-detect via alpha channel assumes upstream texture has alpha > 0 — verify this holds for Vsynth WFG outputs
