# f_util_profile — tasks

_Session anchor. Update at end of every session._

**Plan**: `.specify/f_util_profile/plan.md`
**Build order**: Sequential. Complete each phase before the next.

---

## Expected file layout

```
f_/
  patchers/
    f_util_profile.maxpat       — production bpatcher (Phase 4b rebuild)
  code/
    profile_compute.js          — original single-axis JS (Phase 2, reference)
    profile_rows.js             — row aggregation JS (Phase 4b)
    profile_cols.js             — column aggregation JS (Phase 4b)
  docs/
    f_util_profile.md           — as-built reference (Phase 5)
  .specify/f_util_profile/
    spec.md                     — locked
    plan.md                     — locked
    tasks.md                    — this file
~/Vsynth/patterns/
    util-profile-scratch.maxpat — scratch patch (Phases 1–3, not versioned)
```

---

## Phase 1 — asyncread + JS skeleton

**Goal**: Prove GPU→CPU→GPU round trip works in a Vsynth context.

- [x] T001 Create scratch patch `~/Vsynth/patterns/util-profile-scratch.maxpat` with a Vsynth texture source wired in. Note: scratch patches require a `vs_render` + toggle to initialize the vsynth GL context.
- [x] T002 Add `jit.gl.asyncread vsynth` and wire texture → asyncread (context arg is positional, not @drawto — confirmed from Vsynth source)
- [x] T003 Write `profile_compute.js` skeleton in `f_/code/`: receive matrix, log dimensions and first cell value to Max console
- [x] T004 Wire `js profile_compute.js` downstream of asyncread; confirm JS receives matrix and dimensions are correct
- [x] T005 Confirmed asyncread output format: char (0–255) — verified from vs_envelope.maxpat which divides by 255. JS must normalize. Noted in plan.md.
- [x] T006 Add `jit.gl.texture @adapt 0 @dim 1 128 @name profile_tex` downstream of JS
- [x] T007 JS writes flat 0.5 test matrix to outlet → `jit.gl.texture` → `jit.pwindow @size 512 4`
- [x] T008 Confirmed: 640×640 char 4-plane matrix, pwindow flat grey. Round trip proven.

**Checkpoint**: JS receives real matrix data from asyncread; outputs a visible 1×128 texture.

---

## Phase 2 — aggregation and interpolation

**Goal**: `profile_compute.js` correctly computes the luminance profile.

- [x] T009 Implement luma conversion in `profile_compute.js` (`0.299R + 0.587G + 0.114B`); normalize from char (÷255). Confirmed working.
- [x] T010 Implement slab aggregation: GPU pre-averaging via `jit.gl.pix vsynth @adapt 0 @type char` passthrough; asyncread delivers full-res matrix; JS reads per-row luma. Architecture confirmed via vs_capture source.
- [x] T011 Implement linear interpolation from resolution-length array → 128-length array
- [x] T012 Write interpolated result to 1×128 `jit.matrix` and send to outlet
- [x] T013 Wire test `live.numbox` for `resolution` — deferred; resolution param wired in Phase 4
- [x] T014 Wire WFG3 as upstream texture; profile updates confirmed in pwindow
- [x] T015 Confirm profile responds to upstream texture — confirmed: pwindow pulses in sync with WFG
- [x] T016 Confirm profile responds to spatially varying input (horizontal bands → non-uniform profile strip). Confirmed: pwindow shows dark-bright-dark gradient matching WFG horizontal band structure. Key fixes: jit.dimop @op avg @step 640 1 for row averaging; variable name conflict (resolution var vs function) resolved by renaming to res; post diagnostic confirmed slabs[0]:0.304 slabs[32]:0.995 slabs[63]:0.299.

**Checkpoint**: Profile output visibly reflects upstream texture structure; `resolution` has visible effect. ✅

---

## Phase 3 — bypass (freeze)

**Goal**: Bypass holds last computed profile.

- [x] T017 Add `gate 1 1` between asyncread output and jit.dimop inlet in scratch patch
- [x] T018 Wire bypass toggle → `sel 0 1` → gate open/close
- [x] T019 Verify bypass off: profile updates every frame
- [x] T020 Verify bypass on: profile freezes at last value; `jit.gl.texture` continues outputting frozen texture
- [x] T021 Verify toggling bypass on and off resumes updating correctly

**Checkpoint**: Freeze-on-bypass confirmed in scratch patch. ✅

---

## Phase 4 — bpatcher JSON build

**Goal**: Production `patchers/f_util_profile.maxpat` built by hand as JSON.

- [x] T022 Finalise `f_/code/profile_compute.js` from scratch patch work; confirm it is found by Max from `patchers/` directory
- [x] T023 Resolve `jit.gl.texture @name` collision strategy — deferred; use `profile_tex` for now, known limitation
- [x] T024 Build `patchers/f_util_profile.maxpat` via build_patcher.py with new `util` archetype; hand-wire internal chain in Max
- [x] T025 Add `live.numbox` for `resolution` (int 1–128 default 64) — generated by build script
- [x] T026 Add `live.numbox` for `freq` (int 1–64 default 8) — generated by build script, UI only
- [x] T027 Add `bypass_toggle.js` jsui wired to `== 0` → gate in0
- [x] T028 Add `autopattr @varname profile_autopattr` — generated by build script
- [x] T029 Add `moduleSize.js` chain — generated by build script
- [x] T030 Add title comment and background panel — generated by build script
- [x] T031 Add `parameters` block registering `resolution`, `freq`, `bypass` — generated by build script
- [x] T032 Open bpatcher in Max; confirm all objects present, no errors on load
- [ ] T033 Verify params save and restore correctly via autopattr — **deferred pending Phase 4 rebuild**

**Key discoveries:**
- `jit.gl.texture @dim` takes width×height but texture declaration is `@dim 128 1` not `@dim 1 128` — h/w order differs from jit.matrix convention
- `util` archetype added to build_patcher.py — generates shell only, internal chain hand-wired
- `== 0` used instead of `!` for bypass inversion (! not valid in Max)

**Checkpoint**: Bpatcher opens in Max. Dual-output rebuild required before checkpoint completes.

---

## Phase 4b — dual-output rebuild

**Goal**: Rebuild `f_util_profile.maxpat` with two independent processing
chains (row + column) and two outlets. Replaces single-chain Phase 4.

**JS files:**
- [x] T034 Write `profile_rows.js` — row aggregation. Adapted from working `profile_compute.js`:
  - Input: 1×height matrix (output of `jit.dimop @step 640 1`)
  - Param: `res_rows` message
  - Output: 128×1 `jit.matrix` (luma interpolated across 128 columns)
- [x] T035 Write `profile_cols.js` — column aggregation:
  - Input: width×1 matrix (output of `jit.dimop @step 1 640`)
  - Param: `res_cols` message
  - Output: 1×128 `jit.matrix` (luma interpolated across 128 rows)
- [x] T036 Verify both JS files found by Max from `patchers/` directory — skipped; known-good pattern

**Scratch patch verification:**
- [x] T037 Add second `jit.dimop @op avg @step 1 640` to scratch patch, wire from same asyncread output
- [x] T038 Add `gate 1 1` on col chain, wire same bypass_toggle → `== 0` → both gates
- [x] T039 Wire col chain: asyncread → dimop_col → gate_col → `js profile_cols.js` → `jit.gl.texture @dim 1 128 @name profile_cols_tex`
- [x] T040 Confirm col profile output in `jit.pwindow @size 4 512` responds to vertically varying input
- [x] T041 Confirm row and col profiles are independent (col varies with horizontal structure, row with vertical)

**Bpatcher rebuild:**
- [x] T042 Update `definition.py`: change `resolution` param to `res_rows` + `res_cols`; add second outlet
- [x] T043 Regenerate `f_util_profile.maxpat` shell via `build_patcher.py` with updated definition
- [x] T044 Hand-wire col chain in Max: asyncread → dimop_col → gate_col → profile_cols.js → profile_cols_tex → outlet 1
- [x] T045 Wire bypass_toggle `== 0` to both gate_row and gate_col in0
- [x] T046 Wire `route res_rows res_cols bypass` control dispatch (replacing `route resolution bypass`)
- [x] T047 Open bpatcher in Max; confirm no load errors, both outlets visible

**Checkpoint**: Both outlets active in production bpatcher. ✓

---

## Phase 4c — autopattr verify

**Goal**: Params save and restore correctly in rebuilt bpatcher.

- [x] T048 Close and reopen `f_util_profile` in Max
- [x] T049 Confirm `res_rows`, `res_cols`, `freq` restore to saved values
- [x] T050 Confirm bypass state restores correctly

**Checkpoint**: All params save/restore via autopattr. ✓

---

## Phase 5 — integration test

**Goal**: Both profile outputs independently drive f_masonry modulation inlets.

- [x] T051 Wire `f_masonry` → `f_util_profile`; confirm both outlets produce textures in pwindow
- [x] T052 Wire outlet 0 (row profile 128×1) → courses modulation inlet on a second `f_masonry`
- [x] T053 Confirm visible per-row spatial modulation in downstream masonry
- [x] T054 Wire outlet 1 (col profile 1×128) → columns modulation inlet on same or separate downstream masonry
- [x] T055 Confirm visible per-column spatial modulation independent of row modulation
- [x] T056 Sweep `res_rows` and `res_cols` independently; confirm each affects only its axis
- [x] T057 Sync `freq` to masonry `courses` / `columns`; confirm band alignment on both axes
- [x] T058 Update `README.md`: add `f_util_profile` to status table as ✅ Working
- [x] T059 Write `docs/f_util_profile.md` as-built reference

**Checkpoint**: Visible independent spatial modulation on both axes confirmed in real signal chain. ✅
